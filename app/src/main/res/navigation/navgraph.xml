<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navgraph"
    app:startDestination="@id/sensorActivity">

    <activity
        android:id="@+id/sensorActivity"
        android:name="SensorActivity"
        android:label="SensorActivity" />
    <activity
        android:id="@+id/gestureActivity"
        android:name="com.example.project_ten.GestureActivity"
        android:label="GestureActivity" />
</navigation>